# Baseline Configs

本目录保存官方 baseline 的配置参考。

注意：当前仓库的 Python 代码已经修改过 OBB head、loss 和灰度输入逻辑。因此，即使使用这里的官方架构 yaml，在当前代码上运行时也仍然会经过本地修改版 head/loss。

如果要得到严格官方 YOLOv8n-OBB baseline，需要在独立分支或干净上游代码中运行，并记录 commit。
